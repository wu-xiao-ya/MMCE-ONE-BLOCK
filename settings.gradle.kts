pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven("https://maven.minecraftforge.net/")
        maven("https://maven.cleanroommc.com")
        maven("https://nexus.gtnewhorizons.com/repository/public/")
    }
}

rootProject.name = "MMCE-One-Block"
